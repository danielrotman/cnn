# CNN From Scratch

A Convolutional Neural Network built entirely from scratch in pure Vanilla JavaScript — no libraries or frameworks. Draw circles, squares, and triangles on a canvas and the CNN classifies them in real-time. All math (convolution, ReLU, max pooling, backpropagation, softmax, cross-entropy loss) is hand-written. Includes a dictionary page with 40 CNN/AI terms.
